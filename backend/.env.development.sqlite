# Development Environment Configuration (SQLite)

# Database Configuration (SQLite for easy development)
DATABASE_URL=sqlite:///./finance_dev.db

# Security Configuration
SECRET_KEY=dev-secret-key-not-for-production-replace-in-production
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=60

# API Configuration
API_TITLE=Personal Finance API - Development
API_VERSION=1.0.0-dev
API_DESCRIPTION=A comprehensive personal finance management API (Development)

# Server Configuration
HOST=0.0.0.0
PORT=8000
DEBUG=true

# Environment
ENVIRONMENT=development

# CORS Configuration (Development - Allow all origins)
CORS_ORIGINS=http://localhost:3000,http://localhost:8080,http://localhost:5173,http://127.0.0.1:3000,http://127.0.0.1:8080
CORS_ALLOW_CREDENTIALS=true
CORS_ALLOW_METHODS=*
CORS_ALLOW_HEADERS=*