# Database Configuration
POSTGRES_DB=finance_production
POSTGRES_USER=finance_user
POSTGRES_PASSWORD=your_secure_password_here

# Backend Configuration
SECRET_KEY=your_very_secure_secret_key_at_least_32_characters_long
JWT_SECRET_KEY=your_jwt_secret_key_here_also_32_characters_minimum
BACKEND_PORT=8000

# CORS Configuration
CORS_ORIGINS=https://yourdomain.com,https://www.yourdomain.com

# Optional: External Database (if using managed DB service)
# DATABASE_URL=postgresql://user:password@host:port/dbname

# SSL Configuration (if using Let's Encrypt)
SSL_EMAIL=your-email@domain.com
DOMAIN=yourdomain.com